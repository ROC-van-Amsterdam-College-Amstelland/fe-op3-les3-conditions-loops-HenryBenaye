//javascript code

function cijfer(){
    var maximum = document.getElementById("invoerCijfer").value; 
    console.log(maximum);

        
    for(teller = 1; teller <= maximum; teller++){
        console.log(maximum);
        document.getElementById("resultaat").innerHTML += teller;

    }
}

//maximum getal óphalen'/uitlezen/ value van input bepalen

//maximum variabele gebruiken in for loop

